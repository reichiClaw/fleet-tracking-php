@extends('layouts.app')
@section('content')
<h1 class="mb-4 text-2xl font-bold">{{ $driver->exists ? 'Fahrer bearbeiten' : 'Fahrer anlegen' }}</h1>
<form method="post" action="{{ $driver->exists ? route('drivers.update', $driver) : route('drivers.store') }}" class="grid gap-4 rounded bg-white p-4 shadow md:grid-cols-2">@csrf @if($driver->exists) @method('PUT') @endif
<label>Name<input name="name" value="{{ old('name', $driver->name) }}" required class="mt-1 w-full rounded border p-2"></label><label>Firma<input name="company" value="{{ old('company', $driver->company) }}" class="mt-1 w-full rounded border p-2"></label><label>Abteilung<input name="department" value="{{ old('department', $driver->department) }}" class="mt-1 w-full rounded border p-2"></label><label>Telefon<input name="phone" value="{{ old('phone', $driver->phone) }}" class="mt-1 w-full rounded border p-2"></label><label>E-Mail<input name="email" value="{{ old('email', $driver->email) }}" class="mt-1 w-full rounded border p-2"></label><label class="flex items-center gap-2 pt-7"><input type="checkbox" name="is_active" value="1" @checked(old('is_active', $driver->is_active ?? true))> Aktiv</label><label class="md:col-span-2">Notizen<textarea name="notes" class="mt-1 w-full rounded border p-2">{{ old('notes', $driver->notes) }}</textarea></label><div class="md:col-span-2"><button class="rounded bg-slate-900 px-4 py-2 text-white">Speichern</button></div></form>
@endsection
